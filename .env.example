# Database
# Get a free PostgreSQL database from:
# - Supabase: https://supabase.com
# - Neon: https://neon.tech
# Format: postgresql://user:password@host:port/database?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/music_classifier?schema=public"

# JWT Secret (change this in production!)
# Generate a secure random string for production
JWT_SECRET="your-secret-key-change-in-production"

# NextAuth (if using NextAuth in the future)
NEXTAUTH_SECRET="your-nextauth-secret"
NEXTAUTH_URL="http://localhost:3000"

# API URL (for client-side requests)
# Leave empty for same-origin requests
NEXT_PUBLIC_API_URL=""
